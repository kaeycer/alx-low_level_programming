#include <stdio.h>
#include "main.h"

/**
 * print_sign - prints the sign of the number
 * @n: integer as a parameter
 *
 * Return: 1 if bigger than zero, 0 if equals zero, -1 if less than zero
 */
int print_sign(int n)
{
	int n = 0;

	if (c > 0)
	{
		n += 1;
		printf("+");
	}
	else if (c == 0)
	{
		n += 0;
		_putchar('0);
	}
	else
	{
		n -= 1;
		printf("-");
	}
	return (n);
}
