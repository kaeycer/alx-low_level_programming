#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: first parameter
 * @b: second parameter
 *
 * Return: the product of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
