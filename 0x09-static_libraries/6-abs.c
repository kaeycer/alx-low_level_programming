#include <stdio.h>
#include "main.h"

/**
 * _abs - creates an absolute value of the given parameter
 * @n: parameter of the funct
 *
 * Return: Always positive integers
 */
int _abs(int n)
{
	if (n > 0)
	{
		n *= 1;
	}
	else if (n == 0)
	{
		n *= 0;
	}
	else
	{
		x *= -1;
	}
	return (n);
}
